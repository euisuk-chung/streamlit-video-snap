[server]
# Allow connections from any network interface (LAN access)
address = "0.0.0.0"
port = 8501

# Enable CORS for cross-origin requests
enableCORS = false
enableXsrfProtection = false

[browser]
# Don't automatically open browser on start
gatherUsageStats = false
