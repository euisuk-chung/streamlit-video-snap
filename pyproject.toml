[project]
name = "streamlit-video-snap"
version = "0.1.0"
description = "A local Streamlit application that extracts and plays audio from YouTube videos"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "streamlit>=1.28.0",
    "requests>=2.31.0",
]

[dependency-groups]
dev = []
